---
- name: git
  homebrew: name=git state=latest

- name: mercurial
  homebrew: name=mercurial state=latest

- name: boot2docker
  homebrew: name=boot2docker state=latest

- name: nodejs
  homebrew: name=nodejs state=latest

- name: dash
  homebrew: name=dash state=latest

- name: tmux
  homebrew: name=tmux state=latest

- name: reattach-to-user-namespace
  homebrew: name=reattach-to-user-namespace state=latest

- name: tmux-osx.conf
  copy: dest=~/.tmux-osx.conf src=tmux-osx.conf backup=yes

# pip install awscli
# git
# mercurial
# nodejs
# boot2docker
# bower
# git config core.excludesfile
# git core.excludesfile

# nginx --with-debug
# virtualbox
# docker
# ~/.bashrc
# ~/.bash_profile
# ~/.gitignore
# sourcetree
# colloquy
# hipchat
# go
# cross compile go for linux, darwin, and windows
# chrome
# firefox
# firebug
# balsamiq
# IntelliJ
# iTerm2

